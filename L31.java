



public class L31 {//Mutable Strings

	public static void main(String[] args) {
//	final	StringBuffer sb1 = new StringBuffer("Sabari ");//Buffer is synchronised but builder is not
//		StringBuffer sb2 = new StringBuffer("Sabari ");//but builder gives more performance by allowing more threads to work same time
//		System.out.println(sb1.equals(sb2));//builder and buffer do not have the equals to method it actually compares the references
//		System.out.println(sb1.append(sb2));//here sb1 is a reference type variable(holding address) we did not change any reference just the value its holding
//		//sb1 = new StringBuffer("hi");here we totally changing the reference to another object so it cause compile  time error because sb1 is final
//		
//	   
		
}
}
   